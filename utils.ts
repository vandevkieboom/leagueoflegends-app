/* 

import { Dispatch, SetStateAction } from 'react';

export const handleSearch = (setSearchBarVisible: Dispatch<SetStateAction<boolean>>) => {
  setSearchBarVisible((prev) => !prev);
};

export const handleFilter = () => {
  alert('Filter button pressed');
};

export const handleSort = (setSortModalVisible: Dispatch<SetStateAction<boolean>>) => {
  setSortModalVisible(true);
};

export const handleSortOption = (
  option: string,
  setSortOption: Dispatch<SetStateAction<string>>,
  setSortModalVisible: Dispatch<SetStateAction<boolean>>
) => {
  setSortOption(option);
  setSortModalVisible(false);
};

export const handleNothing = () => {
  alert('Nothing button pressed');
};

*/
