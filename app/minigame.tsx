import { Text, View } from 'react-native';

const Minigame = () => {
  return (
    <View
      style={{
        flex: 1,
        justifyContent: 'center',
        alignItems: 'center',
      }}
    >
      <Text>Minigame Screen</Text>
    </View>
  );
};

export default Minigame;
